mixin box-actions(link)
  div(
    class="input-group" 
    box-actions=link
  )
    select(class="custom-select")
      option(value='') Chọn hành động...
      option(value='active') Hoạt động
      option(value='inactive') Dừng hoạt động
      option(value='delete') Xóa
    .input-group-append
      button(
        class="btn btn-outline-secondary"
        type='button'
      ) Áp dụng

mixin box-actions2(link)
  div(
    class="input-group" 
    box-actions=link
  )
    select(class="custom-select")
      option(value='') Chọn hành động...
      option(value='active') Hoạt động
      option(value='inactive') Dừng hoạt động
       option(value='restore') Khôi phục
      option(value='delete') Xóa hẳn
    .input-group-append
      button(
        class="btn btn-outline-secondary"
        type='button'
      ) Áp dụng